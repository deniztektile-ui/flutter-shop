name: flutter_shop
description: E-commerce app with in-app purchases for App Store and Google Play
publish_to: 'none'
version: 1.0.0+1

environments:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  in_app_purchase: ^3.2.0
  cached_network_image: ^3.3.3
  go_router: ^14.2.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
